# Scan this file for changes every 30 seconds
# refresh_rate: 30 seconds

appenders:
  # An appender named "stdout" that writes to stdout
  stdout:
    kind: console
    encoder:
      pattern: "{d(%Y-%m-%d %H:%M:%S)(local)} {t}]:{h({([{l}])})} {m}{n}"
      # pattern: "{h({l:<5}} {M} > {m}{n}"

# An appender named "requests" that writes to a file with a custom pattern encoder
#  requests:
#    kind: file
#    path: "log/requests.log"
#    encoder:
#      pattern: "{d} - {m}{n}"

# Set the default logging level to "warn" and attach the "stdout" appender to the root
root:
  level: off
  appenders:
    - stdout

loggers:
  hiivelabs_storage_lib:
    level: debug
    appenders:
      - stdout
    additive: false

#  hiivelabs_chunk_lib::chunk_storage:
#    level: off
#    additive: false
#
#  hiivelabs_storage_lib:
#    level: off
#    additive: false